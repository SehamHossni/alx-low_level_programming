#include <stdio.h>

/**
 * main - Entry Point
 *
 * Description: print all single digit numbers of base 10
 *starting from 0, followed by a new line.
 *
 * Return: 0 (Success)
*/

int main(void)
{
	for (int i = 0 ; i < 10 ; i++)
	{
		printf("%d\n", i);
	}

	return (0);
}
